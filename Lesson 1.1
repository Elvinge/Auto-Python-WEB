import pytest
from rest_library import MyAPIClient

@pytest.fixture(scope='session')
def base_url():
    return 'https://test-stand.gb.ru'

@pytest.fixture
def rest_client(base_url):
    return MyAPIClient(base_url)

@pytest.fixture
def test_data():
    return [
        ('New Post 1', 'Description for post 1', 'Content for post 1'),
        ('New Post 2', 'Description for post 2', 'Content for post 2'),
    ]
